/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com', // Example domain for Unsplash images
      'your-custom-domain.com', // Add your custom domain here
    ],
  },
};

module.exports = nextConfig; 